package bar.calculator

case class bar_last_deep(
                          ticker_id     :Int,
                          bar_width_sec :Int,
                          deep_count    :Int
                        )
